{
  "name": "event-app-root",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install:all": "npm install --prefix event-backend && npm install --prefix event-frontend",
    "dev": "concurrently \"npm run start:dev --prefix event-backend\" \"npm run dev --prefix event-frontend -- -p 3001\"",
    "dev:backend": "npm run start:dev --prefix event-backend",
    "dev:frontend": "npm run dev --prefix event-frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
